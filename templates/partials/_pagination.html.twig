<div class="px-6 py-4 border-t border-gray-200 flex items-center justify-between">
	<div class="text-sm text-gray-500">
		Affichage de
		{# <span class="font-medium">{{ pager|length }}</span> #}
		lignes
	</div>
	{# Pagination #}
	<div class="flex space-x-2">
		{{ pagerfanta(pager) }}
	</div>
</div>

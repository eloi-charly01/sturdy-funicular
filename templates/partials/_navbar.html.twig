<header class="border-b border-border bg-surface">
	<div class="mx-auto flex h-16 items-center justify-between px-4 sm:px-6 lg:px-8">
		<div class="flex items-center gap-8">
			<a href="{{ path('app_home') }}" class="flex items-center text-lg font-bold text-ink">
				<span class="mr-2 flex h-8 w-8 items-center justify-center rounded-md bg-brand-600 text-white">
					<i class="fas fa-tasks text-sm"></i>
				</span>
				TaskManager
			</a>

			<nav class="hidden md:flex" aria-label="Navigation rapide">
				<span class="text-sm text-ink-muted">{{ app.user.firstname ~ ' ' ~ app.user.lastname }}</span>
			</nav>
		</div>

		<div class="flex items-center gap-4">
			{{ include('components/button.html.twig', {
				href: path('app_logout'),
				variant: 'secondary',
				icon: 'fas fa-sign-out-alt',
				label: 'Déconnexion',
				extra_classes: 'hidden sm:inline-flex',
			}) }}
			<a href="{{ path('app_logout') }}" class="text-ink-muted hover:text-ink sm:hidden" title="Déconnexion">
				<i class="fas fa-sign-out-alt"></i>
			</a>
		</div>
	</div>
</header>
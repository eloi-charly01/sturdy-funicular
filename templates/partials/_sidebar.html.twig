<div class="hidden m-4 md:flex md:flex-shrink-0">
	<div
		class="flex flex-col w-64 bg-white">
		<!-- Logo -->
		<div class="flex items-center flex-col justify-center p-4 border-b border-gray-700 space-y-2">
			<div class="relative group">
				<img class="h-20 w-20 rounded-full cursor-pointer" src="https://placehold.co/600x700.png" alt="Profile">
				<div class="absolute inset-0 bg-gray-900 bg-opacity-50 rounded-full opacity-0 group-hover:opacity-100 flex items-center justify-center transition-opacity duration-200">
					<i class="fas fa-camera text-gray-700 text-lg"></i>
				</div>
			</div>
			<div
				class="flex items-center justify-center text-gray-700 text-md font-bold">
				{# {{ app.user.prenom|first|upper }}{{ app.user.nom|first|upper }} #}
				Eloi Charly
			</div>
		</div>


		<!-- Menu principal -->
		<div class="flex flex-col flex-grow px-4 py-4 overflow-y-auto">
			<nav
				class="flex-1 space-y-2">
				<!-- Groupe de menu -->
				<div>
					<h3 class="px-3 text-xs font-semibold text-gray-400 uppercase tracking-wider">Menu Principal</h3>
					<div class="mt-2 space-y-1">
						<a href="{{ path('app_home') }}" class="flex items-center px-3 py-2 text-md font-medium {{ app.current_route == 'app_home' ? 'text-blue-500'	: 'text-gray-500' }} hover:text-gray-700 hover:bg-gray-400 rounded-md">
							<i class="fas fa-home mr-3"></i>
							Tableau de bord
						</a>
						<a href="{{ path('app_project.index') }}" class="flex items-center px-3 py-2 text-md font-medium {{ app.current_route starts with 'app_project.' ? 'text-blue-500'	: 'text-gray-500' }}  hover:text-gray-700 hover:bg-gray-400 rounded-md">
							<i class="fas fa-users mr-3"></i>
							Projets
						</a>
						<a href="{{ path('app_user.index') }}" class="flex items-center px-3 py-2 text-md font-medium {{ app.current_route starts with 'app_user.' ? 'text-blue-500' : 'text-gray-500' }}  hover:text-gray-700 hover:bg-gray-400 rounded-md">
							<i class="fas fa-boxes mr-3"></i>
							Membres
						</a>
						<a href="{{ path('app_task.index') }}" class="flex items-center px-3 py-2 text-md font-medium {{ app.current_route starts with 'app_task.' ? 'text-blue-500' : 'text-gray-500' }}  hover:text-gray-700 hover:bg-gray-400 rounded-md">
							<i class="fas fa-file-invoice-dollar mr-3"></i>
							Taches
						</a>
					</div>
				</div>

				<!-- Autres liens -->
				<div class="mt-8">
					<h3 class="px-3 text-xs font-semibold text-gray-400 uppercase tracking-wider">Autres</h3>
					<div class="mt-2 space-y-1">
						<a href="#" class="flex items-center px-3 py-2 text-sm font-medium text-gray-500 hover:text-gray-700 hover:bg-gray-400 rounded-md">
							<i class="fas fa-cog mr-3"></i>
							Paramètres
						</a>
						<a href="#" class="flex items-center px-3 py-2 text-sm font-medium text-gray-500 hover:text-gray-700 hover:bg-gray-400 rounded-md">
							<i class="fas fa-question-circle mr-3"></i>
							Aide
						</a>
					</div>
				</div>
			</nav>
		</div>
	</div>
</div>
